# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#TODO: Change dev base to fizteh, not falt
development:
  adapter: postgresql
  database: falt_killer_development
  username: postgres
  password: qwerty
  host: localhost
  encoding: UTF8

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.

production:
  adapter: postgresql
  database: killer_fizteh
  username: killer_fizteh
  password: *PASSWORD*
  host: localhost
  encoding: UTF8
